---
import type { AboutContent } from '@/types';
import { getAbout } from '@/utils/content-loader';

const { Bio, ...content }: AboutContent = await getAbout();
---

<div class="lm:flex-row flex w-full flex-col-reverse items-center">
  <div class="lm:w-2/3 lm:pt-0 flex w-full flex-col justify-center space-y-12 pt-10">
    <div class="grid grid-cols-2 items-center gap-2 ms:grid-cols-4">
      {
        content.skills.map((skill) => (
          <div class="rounded-full border border-white/10 py-1 text-center text-sm text-accent">{skill}</div>
        ))
      }
    </div>
    <div class="grid grid-cols-3 gap-2 ms:grid-cols-6">
      {
        content.hobbies.map((hobby) => (
          <div class="flex justify-center">
            <img src={hobby.img} alt={hobby.alt} class="size-10 items-center justify-center" />
          </div>
        ))
      }
    </div>
    <div class="text-lg text-secondary"><Bio /></div>
  </div>
  <div class="lm:w-1/3 lm:justify-end flex w-full items-center justify-center">
    <div class="flex h-full items-center overflow-hidden rounded-b-full">
      <img class="image-shadow lm:h-[300px] lm:w-[218px] h-[200px] w-[150px]" src={content.image} alt="about" />
    </div>
  </div>
</div>

<style>
  .image-shadow {
    filter: drop-shadow(-5px 2px 1px #0296be30) contrast(110%);
  }
</style>
